# Legacy Package Copy

This directory is a legacy/archived copy of the Victor package kept for reference. The active code used by the CLI lives at `victor/` in the repo root. Do not add new features here; port fixes upstream to `victor/` first and only mirror back if absolutely necessary.
