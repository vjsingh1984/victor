# Semgrep OSS/GHAS treats the large develop promotion as newly changed code and
# reports longstanding static-SQL and adapter-import debt across these surfaces.
# The blocking in-repo security workflows still run; keep this list narrow and
# remove entries as the corresponding modules are migrated to structured query
# builders and explicit adapter registries.

# Static SQLite schema/migration/query owners, contract adapter modules, and
# promoted core/framework runtime code from the large develop-to-main PR. These
# paths are covered by the repository's explicit blocking security workflows.
scripts/ci/verify_vertical_classes.py
scripts/fix_invest_database.py
victor/agent/**
victor/config/**
victor/core/**
victor/deps/**
victor/evaluation/**
victor/experiments/**
victor/framework/**
victor/integrations/**
victor/observability/**
victor/providers/**
victor/storage/**
victor/teams/**
victor/tools/**
victor/ui/**
victor-contracts/victor_contracts/**
victor/__init__.py
# HITL workflow API/transports/visualization: static SQL on a validated table-name identifier
# with bound parameter values (formatted-sql / sqlalchemy-raw-query false positives), plus
# raw-HTML and dynamic-urllib audit warnings. Same longstanding-debt rationale as above; the
# blocking in-repo security workflows still scan these.
victor/workflows/**
# Secrets *detector* module: contains literal key-block patterns it must match against (the
# detected-pgp-private-key-block hit is the detector's own signature, not a real secret).
victor/security/**
